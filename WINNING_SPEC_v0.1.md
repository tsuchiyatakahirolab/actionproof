# WebMCP Challenge 勝利仕様書 v0.1

> **更新・2026-08-26: 本版のCutRoom選定と採点順位は撤回済み。現行判断は [v0.2](WINNING_SPEC_v0.2.md)。以下は当時の判断履歴であり、実装指示ではない。**

作成・調査日: 2026-08-26 JST / 所有者: TSUCHIYA LAB / 状態: **企画比較完了・検証対象1案を選定。本実装はHOLD。**

## 0. 決定

**次に検証する1案を CutRoom（仮称）に固定する。CircuitMCP原案は採用しない。**

> 板の傷と動かせない部品を画面で指定すると、AIが同じ画面で条件を守る切り出し計画に組み直す。

英語の用途説明案:

> Mark the damaged area. Your agent repairs the cutting plan without moving the parts you've locked.

これは「受賞が確実」「全ゲートPASS」という決定ではない。5案の中で、短いデモ、同一画面での人とエージェントの共同作業、限定した実装範囲を最も両立しやすいという**企画上の判断**である。採点差は小さく、利用者検証で覆り得る。

最大の反証は既にある。切断最適化・欠陥回避・手動修正・AI支援は既存製品にある。CutRoomが普通の最適化ボタンと比べて条件指定・修正・確認を楽にしないなら、**WebMCPを付けただけの既存機能**となる。その場合は作り込まず、企画を戻す。

今回実施したこと: 公式要件確認、CircuitMCP元案回収、公開競合調査、受賞作3件の記述確認、5案比較、デモ・検証・棄却条件の設計。

今回未実施: アプリ実装、WebMCP実機試験、顧客インタビュー、競合製品の操作比較、第三者による20秒テスト、公開・登録・応募。架空の利用実績や合格結果はない。

## 1. 応募上の固定条件

- 正式締切: **2026-09-03 13:00 PDT = 2026-09-04 05:00 JST**。内部提出目標は9月3日18:00 JST。
- 審査: WebMCP Leverage / Execution / Potential Impact / Creativity & Ambition の4項目が等配点。同点時は記載順に比較。
- 提出: 審査員が使える実稼働URL、英語説明、公開OSSリポジトリ、音声付き・公開YouTube動画3分未満。審査員が実機を操作しない場合も想定する。
- 既存作品を使う場合、8月25日11:00 PT以降のWebMCPによる実質的な拡張だけが評価対象。新旧差分と日時付き履歴を残す。
- 今回は新規プロジェクトとして分離し、Falsify等のコード・非公開研究資産を持ち込まない。
- 受賞枠は10作品。登録人数は応募数ではないため、受賞確率の分母にしない。

出典: [公式ルール](https://webmcp.devpost.com/rules)、[大会概要](https://webmcp.devpost.com/)。

公式FAQには動画不要という古い文と動画必須の文が併存する。動画必須の正式ルールを採る。運用上は締切後に提出版を凍結し、少なくとも審査終了9月21日17:00 PTまで無料で試せる状態を維持する。FAQが求める発表までの凍結にも対応する。年齢・居住地・主催者等との利益相反は本人確認事項として残す。[公式Resources / FAQ](https://webmcp.devpost.com/resources)

## 2. 比較対象を同じ粒度に揃える

各案は「一人の利用者・一つの困り事・一画面・一つの実用的出力」までに絞る。大きな将来市場や機能数では加点しない。

| 案 | 一文の用途 / 最初の利用者 | 60秒の変化案 | WebMCPが担当する共同作業 | 最も強い負け筋 |
|---|---|---|---|---|
| CircuitMCP | 電子工作の初心者が、AIと回路を組み、シミュレーションで直す | 電流制約違反→部品変更→適合 | 回路・部品・測定状態を共有し、人の編集後に再測定 | FluxのAIシミュレーションと外部MCP連携、Wokwi MCPが既存。LED抵抗1個では計算機との差が弱い |
| **CutRoom** | 小規模工房の担当者が、傷・木目・固定部品を保って板取りを修正する | 傷と重なる部品→手持ちの端材へ再配置→検証済み案 | 人が描いた傷・固定位置とAIの提案を同じ版で扱う | 既存の最適化ボタンで同じ体験なら不要。現場の制約を誤る危険 |
| SessionRescue | イベント担当者が、会場閉鎖時に固定セッションを動かさず予定を直す | 部屋が使えない→衝突可視化→代替案 | 時間割のドラッグ編集とエージェント再計画 | Sessionboard AIとWebMCPカレンダー例に近く、予定調整の一変種になりやすい |
| FlowRepair | 小規模事業者が、失敗する自動化フローをサンプルデータで直す | フロー失敗→ノード修正→再実行成功 | 同一キャンバスでノード選択・修正・テスト | n8nに自然言語で作成・修正・デバッグがある。接続先・資格情報の実装負荷も大きい |
| SheetRescue | SaaS導入担当者が、取り込めないCSVを確認しながら修復する | 取り込みエラー→差分承認→検証通過 | 同じ表でセル・列の選択と変換提案を共有 | Flatfileの検証・AutoFix・承認経路、RowsのAI表操作と近い。画面の驚きが弱い |

CircuitMCP原案は、別タスク「Falsify落選分析」にある、5V LED・電流上限・部品追加・配線・シミュレーション・再調整という案を参照した。今回別物に解釈して比較していない。

### 共通採点

1〜5の**主観的な企画評価**。実装済み製品の審査点でも受賞確率でもない。

- 1: 現状の企画では重大な不足。
- 2: 狙いはあるが、強い既存代替または証拠不足。
- 3: 限定した用途なら成立が見込めるが、重要な仮説が残る。
- 4: この締切で強みを実証する具体的な経路がある。
- 5: 現時点で非常に強い比較証拠まである。今回は該当なし。

| 案 | WebMCP Leverage | Executionの実現可能性 | Potential Impactの根拠 | Creativity / 差別化 | 合計 /20 | 判断 |
|---|---:|---:|---:|---:|---:|---|
| CircuitMCP | 4 | 2 | 2 | 2 | 10 | 原案は見送り |
| **CutRoom** | **4** | **3** | **2** | **3** | **12** | **唯一の次段階検証対象** |
| SessionRescue | 3 | 4 | 2 | 2 | 11 | 作りやすいが既存例との距離が足りない |
| FlowRepair | 4 | 3 | 2 | 2 | 11 | 接続・競合の両面で不利 |
| SheetRescue | 3 | 4 | 2 | 1 | 10 | 有用性は説明しやすいが既存機能に近い |

採点理由:

- WebMCP: Circuit/CutRoom/Flowはキャンバス状態を人とAIが読み書きする必然性を設計しやすい。APIの存在だけでは4点を維持できず、実機比較で減点する。
- Execution: Circuitは回路モデルと描画・配線の正しさが重い。CutRoomは矩形・少数部品に限定できるが、切断可能性の検証が必要で3点。既存フロー接続が必要なFlowも3点。
- Impact: **全案2点**。公開競合は課題の存在の手掛かりであり、私たちの顧客・需要・支払意思の証拠ではない。
- Creativity: CutRoomの3点は「ライブで変わる現物制約＋既存の個人エージェント＋検証付き再計画」という組合せの仮説。世界初の主張ではなく、最も不確かな加点。

感度: CutRoomの差別化が1点下がるだけでSessionRescue/FlowRepairと同点。さらに実装難が1点増せば順位は逆転する。従って「圧勝案」ではない。最も安く棄却できる順に、競合操作比較→第三者理解テスト→限定試作へ進む。失敗時に自動的に次点へ乗り換えず、比較表を更新する。

## 3. 直接競合と差分の限界

公開説明を読んだ範囲。各製品のすべての機能・最新版・有料機能を実操作で確認したわけではない。

| 一次資料 | 確認できたこと | 企画への反証 / 残る差分仮説 |
|---|---|---|
| [Flux: Simulate Circuits with a Prompt](https://www.flux.ai/p/blog/simulate-circuits-with-a-prompt) | 自然言語から回路シミュレーション、回路図からの解析 | CircuitMCPの生成→解析という筋は既存 |
| [Flux: MCP公開](https://www.flux.ai/p/blog/summer-2026-updates-flux-mcp-server-and-chat-mode) | 外部エージェントが回路の文脈を取得し、ハードウェア/PCB作業を操作 | 「自分のAIで回路を操作」も既存。WebMCPとremote MCPは別だが、顧客価値は近い |
| [Wokwi MCP](https://docs.wokwi.com/wokwi-ci/mcp-support) | エージェントによるシミュレーション、テスト、仮想ハードウェア操作 | Circuitを教育・組込みへ広げても差別化が自動的に生まれない |
| [Cutting Optimization pro](https://www.optimalprograms.com/cutting-optimization/) | 最適化後の手動配置、板間の移動、傷のある板の処理を紹介 | CutRoomの「傷を避ける」「人が直せる」は単独では差別化にならない |
| [CutList Optimizer](https://asarulabs.com/OurApps/cutlist-optimizer/) | 木目、刃厚、端材優先、インタラクティブな配置、PDF/SVG出力 | 計算結果と出力形式も既存。優位性は条件の受け渡し体験で示す必要がある |
| [CabiAI](https://cabiai.net/landing-en/) | 自然言語/画像からのAI設計と切断配置をうたう | 「木工×AI」は新しくない。宣伝上の速度・利用数は本書では検証済み実績として使わない |
| [Sessionboard AI agenda builder](https://learn.sessionboard.com/studio/ai-agenda-builder) | 条件指定、既存予定を考慮した案、個別または全体の承認 | SessionRescueの中核に直接重複 |
| [WebMCP + Legit calendar](https://github.com/WebMCP-org/webMCP-Legit-exploration) | ブランチ、プレビュー、承認、取り消しを備えた公開例 | 共同編集・承認・undoそのものは新規性の主張にできない |
| [n8n AI Workflow Builder](https://docs.n8n.io/build/ways-of-building-workflows/ai-workflow-builder) | 自然言語でフロー作成・修正・デバッグ | FlowRepairの汎用版は直接競合。隔離テストを足すだけでは不十分 |
| [Flatfile](https://flatfile.com/) / [Rows AI Analyst](https://rows.com/docs/using-the-rows-ai-analyst?category=ai) | 検証・AI修復・レビュー、表の検査/セル操作 | SheetRescueは用途をさらに尖らせないと既存機能の再実装 |

CutRoomが実証すべき差分は次の**一連の体験**であり、個々の機能の新しさではない:

1. CAD用語を入力し直さず、利用者が現物に対応する傷・固定部品を同じ画面で指す。
2. 普段のブラウザーエージェントが、そのページの最新状態から提案を作る。
3. 提案中に利用者が変更すると、古い提案を適用せず、固定条件を保持して更新する。
4. 現場担当者が変更箇所と検証理由を見て採用し、持ち出せる計画を得る。

この体験を競合で同程度以下の手間で再現できる場合、差別化ゲートはFAIL。単にWebMCPを使うという実装上の違いだけではPASSにしない。

### 応募作との重複調査

[大会ギャラリー](https://webmcp.devpost.com/project-gallery)は2026-08-26確認時点で未公開。**「応募作と被らない」は判定不能。**

参照した公開例: [Google Chrome公式デモ一覧](https://github.com/GoogleChromeLabs/webmcp-tools/tree/main/demos)、[OpenAI Showcase](https://developers.openai.com/showcase)。前者には店舗・予約・分析・地図・スマートホーム等、後者には編集・旅行・3D等の例がある。これらは全応募作の一覧ではない。

公開検索は `WebMCP circuit`、`WebMCP cutting/cutlist/nesting`、回路/板取り/予定/フロー/表のAI競合を対象に実施。検索で見つからないことを「存在しない」とは扱わない。8月30日と提出前にギャラリー・GitHub・公開デモを再確認する。公開範囲の比較と未公開作品の不明を併記する。

## 4. 受賞作から要求する水準

以下は作者の公開記述との比較であり、こちらで再現した性能評価でも、審査員の採点理由の断定でもない。

| 受賞作 | 公開記述から確認した強さ | CutRoomが現在負けている点 | 追いつくための証拠 |
|---|---|---|---|
| [Echo Canvas](https://devpost.com/software/echo-canvas-ujzksi) | 空間音響をその場で編集・試聴し、Raw/Simulatedを比較。構造化データを出力 | CutRoomにはまだ操作できる完成品も即時の比較もない | 修正前後を同じ画面で比較し、保存/再読込まで切れ目なく見せる |
| [Mechanica](https://devpost.com/software/xiaoqiang) | 動きを物理状態から生成し、根拠と推論を分け、破損データの検証も記載 | CutRoomの「成立する計画」は現時点で紙上の設計のみ | 見かけだけの配置を通さない独立検証器。刃厚・衝突・固定違反を注入して検出 |
| [veTriage](https://devpost.com/software/veterinary-four-color-triage-app) | 特定の医院・担当者・電話業務に結びつき、実際のパイロット利用を記載 | 工房の利用者も実案件も未確保 | 担当者1名以上による匿名化した実案件の机上評価と、現行手順との比較 |

テスト数・見栄えだけで追いついたとは言わない。特にveTriageとの差は、第三者が実務で使う理由の証拠であり、機能追加では埋まらない。

## 5. CutRoomの最小製品を固定する

### 利用者と課題

- 最初の対象: 小規模な木工/展示什器制作の工房で、矩形パネルの切り出しを準備する担当者。
- 利用場面: 部品表は既にある。板の傷や使える端材が判明し、既に決めた部品の位置・木目を変えずに計画を直したい。
- 課題仮説: 図上の例外を設定画面へ翻訳し直すこと、再最適化で既存の判断が失われること、変更結果の確認に手間がかかる。
- 完了: 利用者が検証結果と差分を確認した計画を保存し、SVG/印刷用表示と部品一覧を持ち出す。
- 未確認: 発生頻度、現行ツールの不満、支払者、使い続ける意向。推測で埋めない。

### 必須範囲

- 単一利用者と一つのエージェントが、同じブラウザータブを使う。
- 同一材料・厚さ、mm単位、軸に平行な矩形部品。上限3枚の材料、24部品、傷3個。
- 数値入力と図の選択が一致する。傷は矩形で指定、木目は回転可/不可、部品位置の固定ができる。
- 手動操作とWebMCPが同じ状態更新・検証処理を通る。
- 提案は元の計画を上書きしない。差分プレビュー、採用、却下、undo、版不一致での再計画。
- 刃厚、材料境界、部品重複、傷との重複、数量、固定位置、許可された回転、切断順序を検証。
- 矩形のギロチン切断に限定し、切断木で成立を示せる候補だけを「検証済み」にする。単に矩形が収まることと、切り出せることは別。
- サンプル/自分で変更した入力、計画JSONの保存・再読込、計画出力まで通す。

### 作らないもの

CNC/G-code、機械操作、発注、決済、写真からの自動採寸、任意形状ネスティング、曲線、3D家具設計、CAD互換全般、材料強度/機械安全の認証、多人同時編集、会員管理、独自チャット画面、汎用AIプラットフォーム。

表示: 計画支援であり、実際の切断前には寸法・材料・加工条件を担当者が確認する。幾何検証を機械安全の保証と呼ばない。

## 6. WebMCPである理由と、そうでなくてもできること

「WebMCPなしでは数学的に不可能」は主張しない。独自APIやremote MCPと状態同期を作れば類似機能は実現できる。大会が求めるのは、WebMCPで共同作業が実質的に良くなること。[公式概要](https://webmcp.devpost.com/)

本案の仮説は、開いているページの選択・未保存変更・固定条件を、そのページが公開する型付きツールで扱い、外部の別コピーを操作しないこと。WebMCPにはページ内で見える実行と現在状態を共有する設計がある。[Chrome公式説明](https://developer.chrome.com/docs/ai/webmcp)

### 比較試験を混同しない

| 比較 | 調べる問い | 揃える条件 | PASSの提案基準（未測定） |
|---|---|---|---|
| A: 手動設定＋同じ最適化ボタン vs AI＋WebMCP | AI自体が役立つか | 同じエンジン・在庫・課題・UI機能 | 3名×2課題の探索試験で正しい完了率を悪化させず、完了時間中央値または設定し直し回数を30%以上改善。失敗も全件掲載 |
| B: DOM操作エージェント vs WebMCPエージェント | WebMCPが役立つか | 同じモデル設定・課題・時間上限・同じアクセス可能なUI | 各条件10回。成功率を悪化させず時間中央値30%以上改善、または失敗が2件以上減少。観測できないトークンを推計しない |
| C: 既存の板取り製品 vs CutRoom | 新しい製品を使う理由があるか | 同じ部品・材料・例外条件、操作者の習熟も記録 | 現場担当者が具体的に減った手間と、切り替える条件を説明できる。既存製品が同等以下の手間なら差分HOLD/FAIL |

数値は事前の意思決定用しきい値であり、測定結果・業界標準ではない。小標本で統計的一般化や「30%生産性向上」を広告しない。成功した試行だけの時間比較もしない。

DOM比較を不利にするためにUIを壊したり、ラベルを消したりしない。Aでは最適化ボタンにも同じ条件・同じエンジンを提供する。WebMCPを最適化アルゴリズムの性能と取り違えない。

## 7. 20秒 / 60秒 / 3分デモ仕様

### 20秒: 視聴者が理解すべきこと

「この板の傷を避けて計画を直したい。人が固定した部品は動かしたくない。AIが図を見ながら修正を手伝う。」

画面は材料キャンバスを主役にする。長いチャット履歴、SDK紹介、会社紹介、抽象的なagent論から始めない。最初から実際に困っている配置を見せる。

### 60秒の核心

| 時間目安 | 画面と操作 | 示す証拠 |
|---|---|---|
| 0–10秒 | 既存の板取り。担当者が傷を矩形で囲み、動かさない部品を固定 | 部品1個が傷と重なる。単なる装飾の赤表示ではなく検証結果 |
| 10–20秒 | 「この部品は動かさず、傷を避けて。端材も使って」とエージェントへ依頼 | ページの現在の版と固定条件を取得する実ツール呼び出し |
| 20–40秒 | エージェントが候補を作り、手持ち端材への移動を提案。差分だけを強調 | 変わった部品ID、保持した位置、検証結果 |
| 40–50秒 | 担当者が端材の使える幅を変更。古い提案が失効し再評価 | stale revisionを拒否。人の変更を上書きしない |
| 50–60秒 | 新条件に適合した候補を確認して採用 | 全部品の成立、固定違反0、変更前後の比較 |

これは**目標の台本**。実行時間・反応は未測定。失敗をわざとAIにさせない。出発点の失敗は現物の傷によって既存計画が使えなくなったこと。AIが一発で良い案を出したら、そのまま記録する。

60秒に収まらない場合、編集で待ち時間を隠して「60秒で解決」と言わない。冒頭の価値提示と実測処理時間を分け、複雑すぎるなら企画を縮約する。

### 公開動画: 目標2分40秒

- 0:00–0:20 問題と失敗。
- 0:20–1:00 AIの介入・人の変更・改善。
- 1:00–1:25 入力を別の値に変え、固定と傷の検証が本当に効くことを見せる。
- 1:25–1:45 ツール呼び出し→同じ画面の更新→版検証を短く示す。
- 1:45–2:10 実施できた比較試験だけを件数・条件付きで報告。未実施なら数値を捏造しない。
- 2:10–2:30 出力→再読込、試せるURL。
- 2:30–2:40 限界と締め。英語TTS可。3分未満にする。

### 紙上の成立例: 合成データ、顧客成果ではない

S1: 1200×600mm。端材O1: 600×300mm。部品P1〜P6は各390×280mm。刃厚3mm。回転なし。

初期位置はS1のx=0/393/786、y=0/283の6区画。P1を固定。傷D1=(x1000,y450,w150,h100)を後から指定するとP6だけが重なる。P6をO1の(0,0)へ移す案では、他の5部品を保持したまま傷を避けられる。

この例の矩形交差・境界・P1不変は調査中に算術チェックした。**製品のソルバー、刃厚処理、切断木、WebMCP経路が実装・検証されたという意味ではない。** 切断木の検証は未実施。

使用部品面積655,200mm² / 入力材料面積900,000mm² = 72.8%。傷や刃厚を除いた歩留まりとは別。単にP6を端材へ動かしただけで「AIが材料費を削減」「最適解」とは言わない。既存ソルバーもこの案を出せる可能性が高く、価値検証は第6節の条件受け渡し・修正手間で行う。

## 8. 技術契約（実装指示ではなく、GO後の設計）

想定構成: TypeScript + 小さなReact/Viteアプリ、SVGキャンバス、Web Worker内の決定論的探索・検証、メモリー上の状態と明示保存JSON。一般的な静的HTTPS配信。ホスティング先は未選定で、契約/課金は今回行わない。

AIはユーザー/審査員の対応ブラウザー側のエージェントを使う設計。アプリ独自の有料モデルAPI・埋め込みチャット・APIキー入力を中核にしない。ただし利用側アカウント、モデル利用枠、対応ブラウザーの可用性は別途実機確認が必要。「誰でも無制限無料」ではない。

2026-08-26に確認した標準案とChrome公式資料では `document.modelContext.registerTool` を用いる。古い `navigator.modelContext` の例をそのまま採用しない。標準は変更中なので実装開始日に再確認し、ブラウザー名・バージョン・APIの検出結果を記録する。[標準案](https://webmachinelearning.github.io/webmcp/) / [Chrome](https://developer.chrome.com/docs/ai/webmcp)

### 最小ツール群

| ツール案 | 入出力の責務 | 書き込み範囲 |
|---|---|---|
| `get_workspace` | 版、材料、部品、固定、傷、現在の違反を取得 | 読み取りのみ。量が多ければ概要と詳細を分離 |
| `propose_constraints` | 自然言語から解釈した条件を型付き差分にする | 提案領域だけ。固定解除・部品寸法変更は暗黙に許さない |
| `plan_layout` | 特定版と確認対象条件で有限の候補を生成 | workerが候補IDを返す。LLMが座標や検証結果をでっち上げない |
| `validate_plan` | 境界/傷/刃厚/木目/数量/固定/切断木を独立に検証 | 読み取り。成立・違反・探索打切りを区別 |
| `preview_plan` | 候補と現行との差分を画面へ表示 | プレビューだけ。採用済み計画を変更しない |

採用・却下・undo・exportは明示的な画面操作から始める。エージェントのツール群に自動採用・機械操作・発注を含めない。ただし、ブラウザー操作権限を持つエージェントはボタンを押せる場合があるため、**「人間だけが承認できる安全境界」とは主張しない**。このMVPは現物や外部システムに作用しない。

### 状態と検証の不変条件

1. 入力に `expectedRevision` を要求。人の編集で版が進めば、計算中候補・表示済み候補とも失効する。
2. 採用直前にも同じ版と全制約を再検証。採用は一括で行い、途中状態を正式計画にしない。
3. 手動操作とツール呼び出しは同じ更新・検証処理を使う。片方だけで固定を抜けられない。
4. 探索器と検証器を分け、出された案が正しいことを検査する。探索器が「解を発見できない」だけで不可能と断言しない。
5. 上限: 1計算2秒を初期目標、worker中断可、候補3件まで。期限超過はbest-knownまたは未解決。最適性を保証しない。
6. 実機計測までは速度目標を達成したと表示しない。エージェント呼び出し回数を減らすため、1部品ずつの大量操作でなく意味のある単位を使う。
7. 不正型、負の寸法、NaN、過大入力、未知ID、不正回転、任意コード、任意URLを拒否。

### プライバシーとセキュリティ

- 初期公開デモは合成データのみ。顧客名・実図面・非公開研究データを含めない。
- local-firstでも、ツール結果として返した寸法や文字列は外部エージェント/モデルへ渡り得る。非送信と宣伝しない。
- JSON取り込みはschemaとサイズ上限で検証。SVG等の生マークアップを入力として実行しない。
- テキスト名はデータとして描画し、命令として扱わない。外部送信や任意fetchツールは設けない。
- readOnlyHint等はヒントであり権限検証の代わりにしない。必要以上のcross-origin公開をしない。
- WebMCP非対応時も手動編集を維持し、「Agent unavailable」を明示。決め打ちアニメーションをライブAIと偽らない。

参考: [Chrome tool security](https://developer.chrome.com/docs/ai/webmcp/secure-tools)。

## 9. 共通6ゲートと追加2ゲート

PASS=要求した証拠あり / HOLD=未検証または不明 / FAIL=比較により不足が判明。自己採点や目標値ではPASSにしない。

| ID | ゲート | 事前に固定する証拠 | 2026-08-26状態 |
|---|---|---|---|
| G1 | 一文で説明できる | 第0節の一文が完成し、G2の回答と一致 | 文案完成。理解の実証はHOLD |
| G2 | 20秒で用途が分かる | 初見5名中4名以上が、誘導なしで利用者・問題・出力を言える。少なくとも2名は対象業務経験者 | HOLD、0名 |
| G3 | 60秒で驚きが起こる | 初見視聴者4/5が具体的な変化を指摘。ライブ連続3回、60秒以内に最初の有効な改善を表示 | HOLD、0回 |
| G4 | 3分で問題→失敗→介入→改善が完結 | 2:40の実録案。失敗・待ち時間・未解決を隠さない。第三者が4要素を説明できる | HOLD、動画なし |
| G5 | 全審査項目に具体的証拠 | 下表の4証拠が提出版に紐づく | HOLD、設計のみ |
| G6 | 競合作品と明確に違う | 最も近い既存製品の同課題操作比較、公開応募作の再確認、違いを利用者が説明 | HOLD、資料比較のみ。ギャラリー未公開 |
| W | WebMCPが装飾ではない | 第6節Bの比較＋人の途中変更を反映する実ツール記録 | HOLD、実機なし |
| B | SaaSへつながる実務上の理由 | 対象担当者1名以上が実案件を提供して評価し、継続利用条件を具体化 | HOLD、0名 |

「既存応募作すべてと非重複」を厳密に要求するなら、ギャラリー非公開中は実装前PASSを出せない。本書はこの要件を勝手に緩めない。判断可能なのは公開範囲での差別化であり、未公開応募作の未知は残る。

### 公式4項目に対する証拠契約

| 審査項目 | 提出版に必要な具体物 | 不合格扱いする状態 |
|---|---|---|
| WebMCP Leverage | ブラウザー/版付きのnative呼び出し記録、状態変化、版不一致再計画、B比較 | shimの手動実行だけ、ツール一覧だけ、動画だけの擬似AI |
| Execution | deployed URLでサンプル→変更→提案→採用→出力→再読込、fresh sessionの試験 | 開発者しか起動できない、happy path以外で壊れる |
| Potential Impact | 匿名化した対象者の机上評価、現行手順、成功/失敗込み計測、使う理由 | 「工房市場が大きい」、作者だけの感想、合成データを顧客成果扱い |
| Creativity & Ambition | 最も近い競合との同一課題比較、人とAIの相互修正が見える60秒 | AI・木工・承認・undoの単語だけで新規性を主張 |

## 10. 次の48時間と棄却条件

**本書だけで本実装GOにはしない。** 特にG2/G6/Wに不足がある。まず実装不要の検証を行う。G3/G4/Wの実機証拠には限定試作が必要であり、企画段階でPASSしたことにはできない。

### フェーズA: 実装前の反証（8月27日中を目標）

1. 紙/静止画の3場面で20秒の用途説明を作る。完成品と偽らない。
2. 同じ課題をCutting Optimization pro等でどう行うか、公開デモと実操作で比較。購入や登録が必要なら費用・権限を確認してから。
3. 対象業務経験者への接点を確保する。ユーザーの既存人脈は推定しない。第三者への連絡は許可を得てから行う。
4. 初見5名テストと担当者1名の課題確認。回答文・所要時間・利用経験・拒否理由を匿名で記録。
5. 既存応募作の全件非重複が必須か、公開範囲の確認＋未知の明示でよいかを決定する。

停止条件: 業務経験者の評価が得られない / 普通の最適化で足りる / 20秒で理解されない / 「傷の指定」自体が現場課題と合わない。いずれかなら本実装せずv0.2で企画を再比較。

### フェーズB: 承認後だけの限定試作（8月28日まで、合計8作業時間上限）

時間配分案: native WebMCP呼び出し2h / 最小の計画・検証器3h / 人の途中編集・stale拒否2h /記録1h。1画面1事例に限定し、試作のための汎用基盤を作らない。

停止条件:

- 対応ブラウザーで実エージェント呼び出しを確認できない。
- 固定部品を保持できない、切断木検証を通せない、計算と表示が一致しない。
- 3回続けて60秒以内の有効な変化を示せない。
- 最適化ボタン比較AでAIの役割がなくなる。
- 独自API課金や新しい大規模基盤がないと主要デモが成立しない。

停止時は「もっと実装すれば良くなる」と継続しない。原因を区分して、範囲縮小で価値が残るかを再審査する。CutRoomの名前や作業量を理由に温存しない。

### GO後の残り日程（条件付き）

| JST | 成果物 | 拒否条件 |
|---|---|---|
| 8/29–8/30 | 完全な1経路、競合再調査、比較A/B開始 | 機能を増やさないと説明できない |
| 8/31 | 実案件の机上評価、失敗込み試験結果、仕様凍結 | 顧客価値が作者の感想だけ |
| 9/1 | fresh session QA、回帰、プライバシー、保存/出力 | デモ入力以外で成立しない |
| 9/2 | 英語動画2:40、README、OSS・応募素材 | 動画と公開アプリが不一致 |
| 9/3 18:00 | 内部提出目標、URL/動画/リポジトリ最終確認 | 必須ゲート未通過を成功扱いしない |
| 9/4 05:00 | 正式締切 | 直前の機能追加なし |

利用者と実機の証拠が足りない場合、「受賞優先の基準は未達」と報告する。応募自体を続けるかは、その時点でユーザーが判断する。勝手に応募を撤回しない。

## 11. SaaS仮説: 受賞と商売を混同しない

- 買い手仮説: 小規模工房の責任者/制作進行担当。顧客は未確保。
- 価値仮説: 材料費の魔法の削減ではなく、例外条件の入力・再計画・確認の手間を減らす。
- 継続利用仮説: 手持ち材料/端材の履歴、繰り返し注文、担当者間の引継ぎが必要なら有料の運用機能に余地がある。これらは大会MVPには作らない。
- 価格: 未設定。対象者が普段支払う製品・頻度・失敗コストを聞くまで売上試算や月額を決めない。
- 公開との両立: 大会版は動くコアと必要資産をOSS公開。後の有料価値は運用・導入・継続データ管理等で検討し、審査員用だけに秘密の中核を隠さない。
- 名称CutRoomは仮称。商標/ドメイン/既存サービスとの名称衝突は未調査。商品化時の別ゲート。

最低限の聞き取り:

1. 最後に板取りを変更した具体的な案件は何か。
2. 何が変わり、現行ツールでどこに手間がかかったか。
3. 今の製品で同じ課題を実際に見せられるか。
4. こちらの案で減る手間は何か。増える手間は何か。
5. 次の案件で使う条件は何か。使わないなら、なぜか。

「便利そう」「面白い」だけではBをPASSにしない。

## 12. QAと証拠の記録方法

実装後の記録は `日時 / 版 / ブラウザー / 条件 / 入力fixture / 生ログ / 結果 / 失敗理由` を残す。採点表やデモの数値をコードへ固定しない。

最低限の回帰ケース: 傷との衝突、刃厚不足、板外、部品重複、欠品、二重配置、固定解除の試行、木目違反、入力数値異常、巨大入力、版競合、undo、JSON往復、対応外ブラウザー、探索打切り、解なしと未発見の区別。

最低限のライブ証拠: native discovery→tool invocation→同じ画面更新。別値の入力でも成功。人が途中変更した場合の古い候補拒否。開発者ツールから直接handlerを呼ぶ試験と区別する。

現時点の検証結果:

- 期限のタイムゾーン換算を確認: 9/3 13:00 -07:00 → 9/4 05:00 +09:00。
- 第7節の合成例について、矩形衝突がP6のみ、移動後の傷衝突0、全矩形の材料内収容、固定P1不変、面積72.8%を算術確認。
- 5案の合計は4項目の単純和。どの案も顧客検証・実装検証済みとはしていない。
- Falsifyの既存ファイルは変更していない。本仕様書は新規の別作業フォルダに保存。

## 13. 他の2大会との分離

AI Defense Lab 2026 / ScamBrake、Google Cloud Agentic AI / CrashTest AIは、この仕様書では待機案件。今回それぞれの最新ルール・事前準備可能範囲を再調査していないため、開催日・資格・既存コード再利用可否をここで確定しない。

次の仕様書でも同じ6ゲートを使う。ただしWebMCPのために他大会用コードを前倒ししない。大会開始前に許される準備と大会期間内に作る部分は、各正式ルールを確認して別に記録する。

## 14. 判断の限界

未公開応募作、審査員の個別評価、全競合の操作性、利用者の支払意思は不明。受賞確率は推定しない。「負ける理由が一つもない」ことは証明できないため、**確認できた負け筋に棄却条件を置き、未確認を隠さない**ことを運用原則にする。

本書が完成したことと、プロダクトが受賞水準に達したことは別。現時点の決定は **CutRoomを検証する / 本実装はまだ始めない**。
